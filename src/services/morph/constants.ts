/**
 * Default configuration values for Morph Fast Apply
 */
export const DEFAULT_MORPH_SETTINGS = {
	morphEnabled: false,
	morphBaseUrl: "https://api.morphllm.com/v1",
} as const

/**
 * Default timeout for Morph API requests in milliseconds
 */
export const DEFAULT_MORPH_TIMEOUT_MS = 30000

/**
 * Morph model name for Fast Apply
 */
export const MORPH_FAST_APPLY_MODEL = "morph-v3-large"
